<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/screen_padding"
    android:background="@color/background_light_green"
    android:gravity="center_horizontal"
    tools:context=".HiveInfoActivity">

    <TextView
        android:id="@+id/infoTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Інформація Вулик №1"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/title_dark_green"
        android:layout_marginBottom="@dimen/spacing_small"/>

    <!-- Контейнер для відображення записів (ScrollView для прокрутки) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@drawable/rounded_background"
        android:padding="@dimen/content_padding"
        android:layout_marginBottom="@dimen/spacing_medium"
        android:elevation="2dp">

        <LinearLayout
            android:id="@+id/notesDisplayArea"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">
            <!-- Записи будуть додані програмно -->
        </LinearLayout>
    </ScrollView>

    <!-- НОВЕ: Контейнер для кнопок "Новий запис" та "Мікрофон" -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="@dimen/spacing_small">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/newNoteButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/new_record_button_text"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/add_note_button_color"
            android:paddingVertical="8dp"
            app:cornerRadius="12dp"
            android:stateListAnimator="@null"
            android:elevation="4dp"
            android:layout_marginEnd="@dimen/spacing_small" />

        <ImageButton
            android:id="@+id/microphoneBtn"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:background="@drawable/microphone_button_background"
            android:contentDescription="Голосовий ввід"
            android:elevation="@dimen/button_elevation"
            android:scaleType="centerInside"
            android:src="@drawable/ic_mic"
            android:stateListAnimator="@null" />
    </LinearLayout>

    <!-- Кнопки "Матка" та "Примітки" -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_horizontal"
        android:layout_marginBottom="@dimen/spacing_small">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/queenBtn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Матка"
            android:textSize="@dimen/nav_button_text_size"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/nav_button_color"
            android:paddingVertical="@dimen/nav_button_padding_vertical"
            android:paddingHorizontal="@dimen/nav_button_padding_horizontal"
            app:cornerRadius="@dimen/nav_button_corner_radius"
            android:elevation="@dimen/nav_button_elevation"
            android:stateListAnimator="@null"
            android:layout_marginEnd="@dimen/spacing_small"
            android:onClick="openQueenInfo" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/notesBtn"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Примітки"
            android:textSize="@dimen/nav_button_text_size"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/nav_button_color"
            android:paddingVertical="@dimen/nav_button_padding_vertical"
            android:paddingHorizontal="@dimen/nav_button_padding_horizontal"
            app:cornerRadius="@dimen/nav_button_corner_radius"
            android:elevation="@dimen/nav_button_elevation"
            android:stateListAnimator="@null"
            android:layout_marginStart="@dimen/spacing_small"
            android:onClick="openNotesInfo" />
    </LinearLayout>

</LinearLayout>